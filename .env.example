# Remote WordPress install used by the Vite dev proxy and the
# integration test suite. Point it at any site running the plugin.
PC_REMOTE_URL=

# Set to 1 when the remote uses a self-signed certificate (.local dev domains).
PC_ALLOW_SELF_SIGNED=1
